// write tests for login.js

describe('login.js', () => {
    it('should return the token if the authentication is successful', () => {
        const user = {
            'mail':'test@example.com',
            'password':'testpassword'
        }
    })
})